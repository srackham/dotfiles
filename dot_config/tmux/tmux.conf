set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

# Set Ctrl-a as second prefix
set-option -g prefix2 C-a  
bind-key C-a send-prefix -2

# Custom key bindings
bind-key -n M-p command-prompt
bind-key -n M-s split-window -vc "#{pane_current_path}"     # Vertical split
bind-key -n C-M-s split-window -hc "#{pane_current_path}"   # Horizontal split
bind-key -n M-l select-pane -t :.+                          # Next pane
bind-key -n M-h select-pane -t :.-                          # Previous pane
bind-key -n C-M-l next-window
bind-key -n C-M-h previous-window
bind-key -n C-M-c kill-window